import { EnterkeysDirective } from './enterkeys.directive';

describe('EnterkeysDirective', () => {
  it('should create an instance', () => {
    const directive = new EnterkeysDirective();
    expect(directive).toBeTruthy();
  });
});
