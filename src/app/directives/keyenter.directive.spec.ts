import { KeyenterDirective } from './keyenter.directive';

describe('KeyenterDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyenterDirective();
    expect(directive).toBeTruthy();
  });
});
