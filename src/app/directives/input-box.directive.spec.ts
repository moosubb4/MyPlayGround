import { InputBoxDirective } from './input-box.directive';

describe('InputBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new InputBoxDirective();
    expect(directive).toBeTruthy();
  });
});
